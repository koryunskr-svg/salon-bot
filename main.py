(venv) root@5861467-mu663385:~/salon-bot# sed -n '4610,4625p' main.py
    # 3. Ищем записи пользователя
    found = []
    for r in records:
        # ← ПРОПУСКАЕМ пустые/некорректные записи
        if not r or not isinstance(r, list):
            continue  # ← КЛЮЧЕВОЕ ИСПРАВЛЕНИЕ
        # Ищем по chat_id
        if (
            len(r) > 13
            and str(r[13]).strip() == str(user_id)
            and str(r[8]).strip() == "подтверждено"
        ):
            print(f"🔍 НАЙДЕНА ЗАПИСЬ: ID={r[0]}, Дата={r[6]}, Статус={r[8]}")

            # ← ПРОВЕРКА ДАТЫ (может быть датой или строкой)
            date_cell = r[6] if len(r) > 6 else ""
(venv) root@5861467-mu663385:~/salon-bot#

